export enum FieldType {
  String = 'String',
  Text = 'Text',
  Int = 'Int',
  Float = 'Float',
  Decimal = 'Decimal',
  Boolean = 'Boolean',
  DateTime = 'DateTime',
  Json = 'Json',
}

export enum SearchTypeEnum {
  EQ = 'EQ',
  LIKE = 'LIKE',
}

export enum FormItemTypeEnum {
  Input = 'input',
  Textarea = 'textarea',
  Number = 'number',
  Select = 'select',
  Radio = 'radio',
  Datetime = 'datetime',
  Switch = 'switch',
}
